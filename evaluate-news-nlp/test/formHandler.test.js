import { handleFormSubmit } from "../src/client/js/formHandler";


describe("Testing form submission functionality", () => {
    test("handleFormSubmit should be defined", () => {
        expect(handleFormSubmit).toBeDefined();
    });
});
